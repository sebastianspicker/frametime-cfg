// net_bad.cfg - frametime.cfg
// Everything wrong at once: high ping + jitter + packet loss.
//
// Connection profile: 60ms+ ping AND unstable (jitter, loss, or both)
// Examples: satellite internet (Starlink in poor conditions, HughesNet),
//           mobile roaming (international SIM), tethering with weak signal,
//           hotel/hostel Wi-Fi, severely congested ISP during peak hours
//
// HOW TO USE:  exec net_bad  (in CS2 console)
//              Reset when back on a stable connection: exec net_stable
//
// WHAT THIS DOES:
//   This is an advanced rescue profile, not a general default.
//
//   The profile requests a three-tick network buffer, an interpolation ratio of
//   two, and a longer disconnect timeout. These are experimental choices. The
//   repository has no packet trace that establishes their timing or effect on
//   another route or CS2 build.
//
// Compare telemetry and input response with net_stable on the target route.

cl_interp_ratio "2"
cl_interp "0"
cl_net_buffer_ticks "3"
cl_net_buffer_ticks_use_interp "1"
cl_tickpacket_desired_queuelength "2"
cl_timeout "60"
net_client_steamdatagram_enable_override "1"

echo "[frametime.cfg] net_bad - experimental high-ping unstable profile"
