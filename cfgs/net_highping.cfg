// net_highping.cfg - frametime.cfg
// High base latency, but the route itself is stable (consistent ping, low jitter).
//
// Connection profile: 60ms+ ping, stable routing, low jitter, low loss
// Examples: connecting to a server in another region, Starlink with clear sky,
//           ISP with inefficient routing but consistent behavior
//
// HOW TO USE:  exec net_highping  (in CS2 console)
//
// WHAT THIS DOES:
//   This is an advanced rescue profile, not a general default.
//
//   The profile requests a two-tick network buffer, an interpolation ratio of
//   two, and a longer disconnect timeout. These are experimental choices. The
//   repository has no packet trace that establishes their timing or effect on
//   another route or CS2 build.
//
// IF YOU HAVE high ping + jitter or loss:  use net_bad instead
//
// Compare telemetry and input response with net_stable on the target route.

cl_interp_ratio "2"
cl_interp "0"
cl_net_buffer_ticks "2"
cl_net_buffer_ticks_use_interp "1"
cl_tickpacket_desired_queuelength "1"
cl_timeout "60"
net_client_steamdatagram_enable_override "1"

echo "[frametime.cfg] net_highping - experimental high-ping profile"
