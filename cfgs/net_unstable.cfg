// net_unstable.cfg - frametime.cfg
// Jitter and/or packet loss on a connection with acceptable base ping.
//
// Connection profile: ping <60ms but erratic - high jitter (±20ms+), packet loss,
//                     or both simultaneously
// Examples: Wi-Fi with interference, 4G hotspot with decent signal,
//           congested home network (streaming/downloads running), ISP peering issues
//
// HOW TO USE:  exec net_unstable  (in CS2 console)
//              Reset when back on a stable connection: exec net_stable
//
// WHAT THIS DOES:
//   This is an advanced rescue profile, not a general default.
//
//   The profile requests a four-tick network buffer, an interpolation ratio of
//   two, and a longer disconnect timeout. These are experimental choices. The
//   repository has no packet trace that establishes their timing or effect on
//   another route or CS2 build.
//
// IF YOU ALSO HAVE high base ping:  use net_bad instead (smaller buffer)
//
// Compare telemetry and input response with net_stable on the target route.

cl_interp_ratio "2"
cl_interp "0"
cl_net_buffer_ticks "4"
cl_net_buffer_ticks_use_interp "1"
cl_tickpacket_desired_queuelength "2"
cl_timeout "60"
net_client_steamdatagram_enable_override "1"

echo "[frametime.cfg] net_unstable - experimental unstable-route profile"
